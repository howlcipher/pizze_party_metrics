Task: Add Meeting vs. Maker Time Analysis Chart

Description: Analyze calendar data to show the ratio of uninterrupted "Maker Time" versus fragmented "Meeting Time."
Context: A natural extension to the context-switching metrics that provides highly actionable insights for teams.

Implementation Plan:
The ETL pipeline (`/run/media/system/tallgeese/dev/pizza_party_metrics/scripts/etl/metrics_processor.py`) already computes two relevant fields for every data record:
- `focus_hours`: Represents uninterrupted "Maker Time"
- `meeting_overhead`: Represents fragmented "Meeting Time"

Your task is to:
1. Create a new React component at `/run/media/system/tallgeese/dev/pizza_party_metrics/src/components/Charts/MakerVsMeetingChart.tsx`. This component should take the filtered data as a prop (`data`) and render a chart (e.g., a stacked bar chart using Recharts) comparing the average `focus_hours` vs `meeting_overhead` grouped by `work_setup_category` (e.g., Remote-First, Hybrid, Onsite-Heavy).
2. Ensure the chart is accessible (has `.sr-only` descriptions, `role="img"`, and `aria-label`).
3. Import and render the new `MakerVsMeetingChart` component in `/run/media/system/tallgeese/dev/pizza_party_metrics/src/components/Dashboard/Dashboard.tsx` alongside the other charts. It can take up 1 or 2 columns (`col-span-1 xl:col-span-2`).
4. Update `/run/media/system/tallgeese/dev/pizza_party_metrics/src/components/Dashboard/Dashboard.tsx` to include `const MakerVsMeetingChart = lazy(() => import('../Charts/MakerVsMeetingChart'));`
5. Style the chart visually appealing using tailwind css matching the existing charts.

Specific Files to Edit:
- `/run/media/system/tallgeese/dev/pizza_party_metrics/src/components/Dashboard/Dashboard.tsx`
- Create `/run/media/system/tallgeese/dev/pizza_party_metrics/src/components/Charts/MakerVsMeetingChart.tsx`
